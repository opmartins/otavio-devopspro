name: primeira-pipeline
on:
  push:
    branches: ["main"]
  workflow_dispatch:
jobs:
  primeiro-job: 
    name: "Meu primeiro job."
    runs-on: ubuntu-latest
    steps:
      - name: "Minha primeira action"
        run: echo "Hello World !!!"
      - name: "Segunda action"
        run: |
          echo "Você criou seu primeiro Workflow !!!"
          echo "Parabéns !!!"
      - name: "Script Python"
        run: |
          variavel = "Hello Python!!"
          print(variavel)
        shell: python
    segundo-job:
      - name: "Instalação do ambiente NodeJS"
      runs-on: ubuntu-latest
      steps:
        - name: "Verificacão da versão do NodeJS atual"
        run: node --version
